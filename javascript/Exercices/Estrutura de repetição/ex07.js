// Dado um número inteiro n. Sua tarefa é descobrir quantos dígitos este inteiro contém 
// sem usar os métodos Stringou Array!

function sumDigits(digitos) {
    for (let i = 0; i < 10; i++){
        console.log()
        
    }
}